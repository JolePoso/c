#include <stdio.h>

int main(){

    int sum=0, i=10;
    for(int i=20, i>0, i--){
        scanf("%d", &n);
        sum+=n;
        i=--;
    }
        printf("Ukupno: %d\n", sum);
    printf("Prosjecno: %f", (float)sum/10);







}
